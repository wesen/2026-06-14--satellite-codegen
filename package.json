{
  "name": "satellite-js-cpp-transpiler",
  "version": "0.1.0",
  "description": "A focused JS-to-C++ transpiler for scripts that use the satellite-os API described in source/satellite-js.md.",
  "type": "module",
  "bin": {
    "satjs-cpp": "./src/cli.js"
  },
  "scripts": {
    "test": "node --test",
    "transpile:example": "node ./src/cli.js examples/housekeeping.js --out build/housekeeping.cpp",
    "compile:example": "npm run transpile:example && c++ -std=c++20 -Iruntime -Iexamples build/housekeeping.cpp examples/runner.cpp -o build/housekeeping",
    "smoke": "npm test && npm run compile:example && npm run check:example",
    "check:example": "node ./src/cli.js examples/housekeeping.js --check",
    "dump-ir:example": "node ./src/cli.js examples/housekeeping.js --dump-ir"
  },
  "keywords": [
    "satellite-os",
    "transpiler",
    "codegen",
    "cpp"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@babel/parser": "^7.28.5"
  },
  "homepage": "https://example.invalid/satellite-js-cpp-transpiler",
  "files": [
    "src/",
    "runtime/",
    "examples/",
    "docs/",
    "README.md"
  ]
}
